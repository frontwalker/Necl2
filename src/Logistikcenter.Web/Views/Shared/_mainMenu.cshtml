@{    
    var siteMap = SiteMap.RootNode.ChildNodes;
}

<ul id="menu" class="tabmenu">
    
    @foreach (SiteMapNode node in siteMap)
    {
        <li>@Html.ActionLink(@node.Title, "Index", @node.Url)</li>
    }

</ul>